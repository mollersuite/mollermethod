---
import Layout from '../layouts/Layout.astro'
import { InfoBar } from 'fluent-svelte'
---

<Layout title="Deprecated!" description="catay now has a built-in settings UI" center unlisted>
	<InfoBar title="Deprecated!" message="catay now has a built-in settings UI. Use that instead." closable={false} severity="critical"/>
</Layout>
